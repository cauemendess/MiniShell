# Valgrind flags
#  valgrind --trace-children=yes --leak-check=full --show-leak-kinds=all --track-fds=yes --trace-children-skip=''*/bin/*,*/sbin/*'' --suppressions=suppressions.supp ./minishell

# Pode haver um problema em organizar os elementos quando tem um pipe.
# Para entender melhor, vamos quebrar os outputs em 2 momentos, para 2 built-ins diferentes.

# O problema é que o env não envia o que deveria, manda apenas uma linha vazia
# Talvez o problema esteja na forma que a env está sendo tokenizada, ou parseada, quando tem pipe

env | echo $HOME | awk '{print("apenas um teste " $1)}' | wc
echo oi |env | wc | cat

# O problema pode ser com todas as built-ins.
# Esse comando deveria ter o retorno diferente de 0 0 0

echo -n  oi | wc




-------------------------------------------------------------------------------------
# Norma:
# - verificar cd.c: função com mais de 25 linhas
# - verificar tokenizer.c: tem uma função a mais
# - verificar expansion.c: tem uma função a mais
# - verificar syntax_error.c: tem uma função a mais
# - verificar operators: work in progress